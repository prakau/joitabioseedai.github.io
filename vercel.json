{
  "installCommand": "npm ci --prefix api && npm ci --prefix farmassist-src",
  "buildCommand": "cd farmassist-src && npm run build",
  "outputDirectory": ".",
  "functions": {
    "api/*.js": {
      "maxDuration": 60
    }
  },
  "headers": [
    {
      "source": "/api/(.*)",
      "headers": [
        {
          "key": "Cache-Control",
          "value": "no-store"
        }
      ]
    }
  ]
}
